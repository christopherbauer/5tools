apiVersion: apps/v1
kind: Deployment
metadata:
  name: e5-database-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: e5-database-mongo
  template:
    metadata:
      labels:
        app: e5-database-mongo
    spec:
      containers:
        - name: e5-database-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: e5-database-mongo-srv
spec:
  selector:
    app: e5-database-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017